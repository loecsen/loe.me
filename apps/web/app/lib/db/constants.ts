/**
 * Policy version: bump when rules/prompts change so cache hits are only for comparable behavior.
 * Included in decision unique key context_hash.
 */

export const POLICY_VERSION = '2026-01-31-d';
